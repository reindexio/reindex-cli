#!/usr/bin/env node

var cli = require('../lib/index.js');

cli(process.argv.slice(2))
  .then(function () {
    process.exit(0);
  }, function (error) {
    process.stderr.write(error.toString() + '\n');
  });
